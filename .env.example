# URL base da API de produtos.
# Em dev, aponta para o mock (json-server) incluso no projeto.
# Em produção, troque pela URL da sua API real (ex: https://api.vtt.com.br)
VITE_API_URL=http://localhost:3001
